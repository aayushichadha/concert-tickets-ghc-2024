# Sample Fluentd Configuration

# Source: where to read logs from
<source>
  @type forward
  port 24224
  bind "0.0.0.0"
</source>

# Match: output parsed logs to stdout (for testing)
<match **>
  @type stdout
</match>
